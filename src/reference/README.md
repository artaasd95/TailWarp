# CPU Reference Implementations

Slow but correct CPU versions for validation of GPU kernels.

## Purpose

- Unit testing: compare GPU output against CPU reference
- Debugging: when GPU gives unexpected results
- Prototyping: test algorithm logic before CUDA implementation

All files here are single-threaded CPU code with simple, readable implementations.

