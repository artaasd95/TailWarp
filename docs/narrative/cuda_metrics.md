CUDA Kernel Metrics
===================

TailWarp provides CUDA kernels for tail-risk covariance estimation and manifold operations.

See also ``python-api.md`` and ``VALIDATION.md`` in this directory.

Benchmark results are collected via ``runpod/run_benchmarks.sh`` on RunPod GPU pods.
